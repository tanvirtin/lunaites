import { assertStrictEquals, describe, it } from "../../deps.ts";

describe("Scanner", () => {
  describe("parse", () => {
    it("should pass", () => {
      assertStrictEquals(true, true);
    });
  });
});
